modality: video
data:
  raw_dir: data/raw/video
  processed_dir: data/processed/video
training:
  seed: 42
  batch_size: 8
  num_epochs: 10
  learning_rate: 0.0003
model:
  name: baseline_video_mlp
  input_dim: 256
  hidden_dim: 256
  num_classes: 2
