modality: mcq
data:
  raw_dir: data/raw/mcq
  processed_dir: data/processed/mcq
training:
  seed: 42
  batch_size: 64
  num_epochs: 50
  learning_rate: 0.001
model:
  name: baseline_mcq_mlp
  input_dim: 64
  hidden_dim: 128
  num_classes: 2
